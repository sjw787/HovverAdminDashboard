╔══════════════════════════════════════════════════════════════════════════════╗
║           CUSTOMER FILES FILTER - FRONTEND QUICK FIX                         ║
╚══════════════════════════════════════════════════════════════════════════════╝

ISSUE: Filter dropdown shows "customer" but all files are displayed
FIX: Pass prefix parameter to API when fetching images

┌─ THE PROBLEM ────────────────────────────────────────────────────────────────┐
│                                                                              │
│  Current behavior (WRONG):                                                   │
│    User clicks "View Customer Files" → Dropdown shows "customer"            │
│    API call: GET /images/list                                                │
│    Result: Shows ALL files ❌                                                │
│                                                                              │
│  Expected behavior (CORRECT):                                                │
│    User clicks "View Customer Files" → Dropdown shows "customer"            │
│    API call: GET /images/list?prefix=customers/{customer_id}/               │
│    Result: Shows only that customer's files ✅                               │
│                                                                              │
└──────────────────────────────────────────────────────────────────────────────┘

┌─ THE FIX (2 Lines of Code) ──────────────────────────────────────────────────┐
│                                                                              │
│  When calling the images API, add prefix parameter:                          │
│                                                                              │
│  // WRONG (current):                                                         │
│  fetchImages()                                                               │
│                                                                              │
│  // CORRECT (fix):                                                           │
│  fetchImages({ prefix: `customers/${customerId}/` })                        │
│                                                                              │
│  That's it! The backend handles the rest.                                    │
│                                                                              │
└──────────────────────────────────────────────────────────────────────────────┘

┌─ IMPLEMENTATION EXAMPLE ─────────────────────────────────────────────────────┐
│                                                                              │
│  // When "View Customer Files" button is clicked                             │
│  function viewCustomerFiles(customer) {                                      │
│    const prefix = customer.customer_folder + '/';                            │
│    // Result: "customers/abc123-def456-789/"                                 │
│                                                                              │
│    // Fetch with filter                                                      │
│    fetchImages({ prefix })                                                   │
│      .then(data => setImages(data.images));                                  │
│                                                                              │
│    // Update UI                                                              │
│    setSelectedCustomer(customer);                                            │
│    navigate('/images');                                                      │
│  }                                                                           │
│                                                                              │
└──────────────────────────────────────────────────────────────────────────────┘

┌─ API ENDPOINT DETAILS ───────────────────────────────────────────────────────┐
│                                                                              │
│  Endpoint: GET /images/list                                                  │
│                                                                              │
│  Query Parameters:                                                           │
│    • prefix (string, optional) - Filter by S3 path prefix                    │
│    • max_keys (int, optional) - Max results (default: 100)                   │
│                                                                              │
│  Examples:                                                                   │
│    All files:      /images/list                                              │
│    Customer files: /images/list?prefix=customers/abc123.../                  │
│    General files:  /images/list?prefix=general/                              │
│                                                                              │
└──────────────────────────────────────────────────────────────────────────────┘

┌─ FILTER OPTIONS ─────────────────────────────────────────────────────────────┐
│                                                                              │
│  Filter Type       Prefix Value                    Result                    │
│  ───────────────   ─────────────────────────────   ─────────────────────── │
│  All Files         '' (empty string)               All images               │
│  General Files     'general/'                      General folder only      │
│  Customer Files    'customers/{customer_id}/'      That customer's folder   │
│                                                                              │
└──────────────────────────────────────────────────────────────────────────────┘

┌─ CUSTOMER OBJECT ────────────────────────────────────────────────────────────┐
│                                                                              │
│  GET /customers/{id} returns:                                                │
│  {                                                                           │
│    "customer_id": "abc123-def456-789",                                       │
│    "customer_folder": "customers/abc123-def456-789",  ← USE THIS!           │
│    "email": "...",                                                           │
│    "name": "...",                                                            │
│    ...                                                                       │
│  }                                                                           │
│                                                                              │
│  To get prefix: customer.customer_folder + '/'                               │
│                                                                              │
└──────────────────────────────────────────────────────────────────────────────┘

┌─ TESTING ────────────────────────────────────────────────────────────────────┐
│                                                                              │
│  1. Open browser DevTools (F12) → Network tab                                │
│  2. Click "View Customer Files" for a customer                               │
│  3. Look for request to /images/list                                         │
│  4. Check query string:                                                      │
│     ❌ Wrong: /images/list (no prefix)                                       │
│     ✅ Correct: /images/list?prefix=customers/abc123.../                     │
│                                                                              │
└──────────────────────────────────────────────────────────────────────────────┘

┌─ DROPDOWN FILTER HANDLER ───────────────────────────────────────────────────┐
│                                                                              │
│  function handleFilterChange(filterType, customerId) {                       │
│    let prefix = '';                                                          │
│                                                                              │
│    if (filterType === 'all') {                                               │
│      prefix = '';                                                            │
│    } else if (filterType === 'general') {                                    │
│      prefix = 'general/';                                                    │
│    } else if (filterType === 'customer' && customerId) {                     │
│      prefix = `customers/${customerId}/`;                                    │
│    }                                                                         │
│                                                                              │
│    fetchImages({ prefix }).then(data => setImages(data.images));             │
│  }                                                                           │
│                                                                              │
└──────────────────────────────────────────────────────────────────────────────┘

┌─ COMMON MISTAKES ───────────────────────────────────────────────────────────┐
│                                                                              │
│  ❌ prefix = `customer/${id}/`     (Missing 's' - should be 'customers')    │
│  ❌ prefix = `customers/${id}`     (Missing trailing slash)                 │
│  ✅ prefix = `customers/${id}/`    (CORRECT!)                                │
│                                                                              │
│  ❌ Filter images on frontend after fetching all (slow, wastes bandwidth)   │
│  ✅ Pass prefix to API, let backend filter (fast, efficient)                │
│                                                                              │
└──────────────────────────────────────────────────────────────────────────────┘

═══════════════════════════════════════════════════════════════════════════════
SUMMARY: Add prefix parameter when calling fetchImages() - that's all! 🎯
═══════════════════════════════════════════════════════════════════════════════

For complete details: CUSTOMER_FILES_FILTER_ISSUE.txt
